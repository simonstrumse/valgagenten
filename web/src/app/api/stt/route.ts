import { NextResponse } from "next/server";

export async function POST() {
  // Placeholder: return dummy text until Whisper integration is added
  return NextResponse.json({ text: "(transkribert) Din lyd er mottatt – tekststøtte kommer." });
}
